import styled from 'styled-components'

export const ContentContainer = styled.div`
  max-width: 65vw;
  margin: 0 auto;

  @media (max-width: 950px) {
    max-width: 100vw;
    padding: 0 1rem;
  }
`
