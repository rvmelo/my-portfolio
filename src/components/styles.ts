import styled from 'styled-components'

export const ContentContainer = styled.div`
  max-width: 65vw;
  margin: 0 auto;
  padding: 3rem 0;

  @media (max-width: 950px) {
    max-width: 100vw;
    padding: 3rem 1rem;
  }
`
